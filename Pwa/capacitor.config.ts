import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mfnizam.eki',
  appName: 'Eki App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
